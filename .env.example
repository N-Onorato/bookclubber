# Database Configuration
# Local development:
DATABASE_PATH=./bookclubber.db
DATA_DIR=./data

# Railway/Production (set these in Railway environment variables):
# DATABASE_PATH=/app/data/bookclub.db
# DATA_DIR=/app/data
# SESSION_SECRET=your-random-secret-here
# NODE_ENV=production

# Session Secret (REQUIRED - change in production!)
SESSION_SECRET=your-session-secret-change-in-production

# Environment
NODE_ENV=development

# Google Books API (optional - enables fallback book search and metadata enrichment)
# Get an API key from: https://console.cloud.google.com/apis/credentials
GOOGLE_API_KEY=your-google-books-api-key

# Debug Features (optional - enables debug console at /dashboard/debug)
ENABLE_DEBUG_FEATURES=false
